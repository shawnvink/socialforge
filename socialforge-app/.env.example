# SocialForge - API Configuration
# Copy this file to .env.local and add your keys:
#   cp .env.example .env.local

# Required: at least one AI provider
# Option 1: Anthropic (direct access to Claude models)
ANTHROPIC_API_KEY=sk-ant-your-key-here

# Option 2: OpenRouter (access to 100+ models including Claude, GPT-4, Gemini)
# OPENROUTER_API_KEY=sk-or-your-key-here

# Convex (auto-configured when you run: npx convex dev)
# CONVEX_DEPLOYMENT=dev:your-deployment
# NEXT_PUBLIC_CONVEX_URL=https://your-deployment.convex.cloud
